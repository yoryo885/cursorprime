# Videos TikTok

Sistema de **proyectos creados** — shorts verticales 9:16.

## Flujo
```
tema/hook → guion → render (slideshow|animado) → video listo → (publicar)
```

Pipeline Cursor: `hooks-redes` → `guion-a-video` → `creador_imagenes_main.py` (`video.modo`) → `captions-redes`.

**Sin API TikTok** por ahora (`sin_api: true`). Publicar queda en mock.
